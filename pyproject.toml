[project]
name = "AbsoluteUnit"
version = "0.1.0"

requires-python = ">=3.13"
dependencies = [
  "disnake",
  "pint",
  "result",
  "rich",
  "python-dotenv",
  "pydantic",
  "pydantic-settings",
  "tomlkit",
]

[project.optional-dependencies]
test = [
  "pytest"
]
